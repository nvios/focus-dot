#ifndef STATE_MANAGER_H
#define STATE_MANAGER_H

/* #include "State.h"
#include <vector>

class StateManager
{
public:
    StateManager();
    void init(State *initialState);
    void update();
    void handleInput(ButtonDriver& buttons);
    void changeState(State *newState);

private:
    State *_currentState;
    std::vector<State *> _states;
}; */

#endif